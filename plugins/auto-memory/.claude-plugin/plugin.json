{
  "name": "auto-memory",
  "description": "Session observability + memory/knowledge maintenance. A lightweight statusline shows tokens, cost, context % and rate-limit usage; a SessionEnd hook logs per-session token totals; and the /mdv-wrap-session skill (run manually before /clear) routes what is worth keeping into rules or memories and refreshes knowledge files using the live session — no headless subagents, no separate API cost.",
  "version": "0.2.0",
  "author": { "name": "dvomar" },
  "license": "MIT"
}
