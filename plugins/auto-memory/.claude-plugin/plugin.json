{
  "name": "auto-memory",
  "description": "Session observability + memory/knowledge maintenance. A lightweight statusline shows cumulative tokens, cost, context %, and 5h rate-limit usage; a SessionEnd hook logs per-session token totals; and the /wrap-session skill (run manually before /clear) proposes memories and refreshes knowledge files using the live session — no headless subagents, no separate API cost.",
  "version": "0.2.0",
  "author": { "name": "dvomar" },
  "license": "MIT"
}
